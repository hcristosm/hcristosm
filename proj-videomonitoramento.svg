<svg xmlns="http://www.w3.org/2000/svg" width="620" height="204" viewBox="0 0 620 204">
  <style>
    text {
      font-family: ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, 'Liberation Mono', monospace;
      font-size: 13px;
      font-weight: 600;
      fill: #4a2e18;
    }
    @media (prefers-color-scheme: dark) {
      text { fill: #f2dcb0; }
    }
  </style>
  <text x="2" y="15">My master&#x27;s thesis at IG-UNICAMP. The question was whether a cheap camera can</text>
  <text x="2" y="35">catch the moment a vegetated slope on the Serra do Mar starts moving. An</text>
  <text x="2" y="55">OpenCV pipeline tracks 40mm targets frame by frame on a plain CPU, using Canny</text>
  <text x="2" y="75">edges, a circularity filter and a 4px search lock so targets do not get</text>
  <text x="2" y="95">swapped when something blocks the view. I ran Meta SAM 2 zero shot over the</text>
  <text x="2" y="115">same footage, with DBSCAN grouping the masks, because I wanted a second</text>
  <text x="2" y="135">opinion that was not mine. The size is what makes it plausible in the field:</text>
  <text x="2" y="155">30 minutes of video is 113MB, the coordinates it comes down to are about 20MB.</text>
  <text x="2" y="175">One site and a limited set of runs, so it argues for feasibility, not much</text>
  <text x="2" y="195">more.</text>
</svg>
