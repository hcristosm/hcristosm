<svg xmlns="http://www.w3.org/2000/svg" width="620" height="144" viewBox="0 0 620 144">
  <style>
    text {
      font-family: ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, 'Liberation Mono', monospace;
      font-size: 13px;
      font-weight: 600;
      fill: #4a2e18;
    }
    @media (prefers-color-scheme: dark) {
      text { fill: #f2dcb0; }
    }
  </style>
  <text x="2" y="15">Batch image upscaling with Real-ESRGAN on your own machine. Point it at a</text>
  <text x="2" y="35">file, a folder or a zip. GFPGAN face restoration if you want it. It looks for</text>
  <text x="2" y="55">your GPU and falls back to CPU, works through large images in tiles so it does</text>
  <text x="2" y="75">not run out of memory, and copies inputs to a temp workspace so the originals</text>
  <text x="2" y="95">are never touched. There is a Docker image if you would rather not install</text>
  <text x="2" y="115">PyTorch. It was a Colab notebook first and the rewrite has not been tested</text>
  <text x="2" y="135">everywhere yet.</text>
</svg>
