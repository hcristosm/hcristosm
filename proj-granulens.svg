<svg xmlns="http://www.w3.org/2000/svg" width="620" height="144" viewBox="0 0 620 144">
  <style>
    text {
      font-family: ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, 'Liberation Mono', monospace;
      font-size: 13px;
      font-weight: 600;
      fill: #4a2e18;
    }
    @media (prefers-color-scheme: dark) {
      text { fill: #f2dcb0; }
    }
  </style>
  <text x="2" y="15">Measures grains from a photo. Gaussian blur, Otsu threshold, then a distance</text>
  <text x="2" y="35">transform feeds Watershed to pull apart particles that are touching. For every</text>
  <text x="2" y="55">grain you get area, equivalent and Feret diameters, aspect ratio and</text>
  <text x="2" y="75">sphericity, plus the D10, D50 and D90 for the sample. It writes out a colored</text>
  <text x="2" y="95">overlay, the PSD curve, a CSV per particle and a summary JSON. Runs as a CLI</text>
  <text x="2" y="115">or as a Python API. It is classical computer vision, so it wants reasonably</text>
  <text x="2" y="135">lit, reasonably separated grains.</text>
</svg>
